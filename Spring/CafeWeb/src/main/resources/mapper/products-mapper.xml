<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycom.myapp.product.dao.ProductDao">

  <select id="findById" parameterType="int" resultType="com.mycom.myapp.product.dto.ProductDto">
    select id, name, category, price
    from Products
    where id = #{id}
  </select>

  <select id="findAllProducts" resultType="com.mycom.myapp.product.dto.ProductDto">
    select id, name, category, price
    from Products
    <if test="keyword != null and keyword != ''">
      WHERE (name LIKE CONCAT('%', #{keyword}, '%')
      OR category LIKE CONCAT('%', #{keyword}, '%'))
    </if>
    ORDER BY id DESC
    LIMIT #{pageSize} OFFSET #{offset}
  </select>

  <select id="countAll" resultType="int">
    select count(*)
    from Products
    <if test="keyword != null and keyword != ''">
      WHERE (name LIKE CONCAT('%', #{keyword}, '%')
      OR category LIKE CONCAT('%', #{keyword}, '%'))
    </if>
  </select>

  <select id="deleteById" parameterType="int">
    DELETE FROM Products
    WHERE id = #{id}
  </select>

  <insert id="registerProduct" parameterType="com.mycom.myapp.product.dto.ProductDto"
    useGeneratedKeys="true"
    keyProperty="id">
    INSERT INTO Products (name, category, price, stock_manage)
    VALUES (#{name}, #{category}, #{price}, #{stockManage})
  </insert>

</mapper>